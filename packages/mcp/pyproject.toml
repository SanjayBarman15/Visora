[project]
name = "mova-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "mova-schemas",
  "mova-rag",
  "mcp>=1.0"
]

[tool.uv.sources]
mova-schemas = { workspace = true }
mova-rag = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
