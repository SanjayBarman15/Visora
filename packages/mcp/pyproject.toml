[project]
name = "visora-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "visora-schemas",
  "visora-rag",
  "mcp>=1.0"
]

[tool.uv.sources]
visora-schemas = { workspace = true }
visora-rag = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
