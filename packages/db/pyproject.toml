[project]
name = "visora-db"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "supabase>=2.0",
  "redis>=5.0",
  "asyncpg>=0.29"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
